# Apps

Local generated mini apps go here.

This directory is intentionally ignored by git, except for this README and `.gitkeep`, because generated apps often contain private data, customer-specific logic, screenshots, embedded CSV/JSON or one-off experiments.

If an app should become a public example, move a cleaned version to `examples/`.
